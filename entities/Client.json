{
  "name": "Client",
  "description": "A client in the system with construction sites",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the client"
    },
    "is_active": {
      "type": "boolean",
      "description": "Whether the client is active",
      "default": true
    }
  }
}