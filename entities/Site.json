{
  "name": "Site",
  "description": "A construction site belonging to a client",
  "properties": {
    "client_id": {
      "type": "string",
      "description": "The ID of the associated client (FK→clients.id)"
    },
    "site_name": {
      "type": "string",
      "description": "The name of the construction site"
    },
    "region_type": {
      "type": "string",
      "description": "The region type (eilat/outside_eilat)",
      "enum": ["eilat", "outside_eilat"]
    },
    "contact_name": {
      "type": "string",
      "description": "The contact person name at the site"
    },
    "contact_phone": {
      "type": "string",
      "description": "The contact person phone number"
    },
    "is_active": {
      "type": "boolean",
      "description": "Whether the site is active",
      "default": true
    }
  }
}